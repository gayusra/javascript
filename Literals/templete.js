// template literals -- introduced in 2015 with or ES6

let fname = 'Ram'
let lname = 'babu'
let city = 'chennai'
//console.log(fname + ' ' + lname + 'lives in ' + city )//using this for so difficult


//another method -- template literals
let msg = `${fname} ${lname} lives in ${city}` //using back tick and doller
console.log(msg)

